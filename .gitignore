# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# venvs
.venv/
.venv-*/
venv/
env/

# models / weights (never commit — huge, and not ours to redistribute)
# NOTE: anchored to repo root with a leading slash so it does NOT match the
# source package mlx_mtp/models/ (the vendored Qwen3.5 model code).
*.safetensors
*.gguf
*.bin
/models/
*-MLX-*/
*-oQ8/
*-DFlash/

# os / editor
.DS_Store
.idea/
.vscode/

# local benchmark outputs
/benchmark*.json
